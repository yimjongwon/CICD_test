# ============ Terraform ============
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
*.tfvars
*.tfvars.json
.terraformrc
terraform.rc
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# ※ .terraform.lock.hcl 은 커밋(프로바이더 버전 고정) >> .gitignore 포함 금지

# ---- apply 생성물 / 개인값 (커밋 금지) ----
# terraform apply 시 자동 생성
# terraform.tfvars 에 비밀값(키 경로 등)을 넣는다면 커밋에서 제외.
infra/terraform/terraform.tfvars
infra/terraform/hcl/backend.hcl   
infra/ansible/inventory.yml       
infra/ansible/ansible.cfg
infra/ansible/group_vars/database.yml
*.auto.tfvars

# ============ 비밀 · 키 ============
*.pem
*.pem.pub
*.key
.env
.env.*
secrets.*
.tailscale_key
.dockerhub_token
.docker_config
.aws/
monitoring/.env
!monitoring/.env.example

# ============ Python ============
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# ============ OS / 에디터 ============
.DS_Store
Thumbs.db
.idea/
.vscode/
memo.md
# ============ 프로젝트별 ============
# Recovery Controller
monitoring/recovery/logs/*.log


monitoring/recovery/logs/recovery_state.json
monitoring/recovery/logs/recovery_state.tmp

# Terraform remote backend config (local)
infra/terraform/backend.hcl
*.retry
